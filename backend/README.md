# Backend

This is a simple API that will mine a set of reasons to say `Yes` or `No` (depending on which API
endpoint is called).  This was inspired by the `Yes as a Service` and `No as a Service` project.

It is deliberately trivial as the main focus of this larger repo is to work out some thoughts on 
mono-repo management w/ Github Actions and CDK.